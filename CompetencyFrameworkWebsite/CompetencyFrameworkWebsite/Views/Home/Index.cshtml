@{
    ViewBag.Title = "Home Page";
}

<form method="post" action="">
    <div style="padding:10px">
        <select>
            @{
                foreach (var technology in @ViewBag.test)
                {
                    <option value="technology">@technology</option>
                }
                
            }
        </select>
        <input type="submit" class="btn btn-default" value="Select">

    </div>
</form>